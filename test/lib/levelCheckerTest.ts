import LevelChecker from "../../src/lib/levelChecker"
import * as Chai from "chai"

describe("LevelCheckerTest", () => {

    let toTest : LevelChecker;

    // Call one of the many function that are in the Mocha framework
    beforeEach(() => {
        toTest = new LevelChecker();
    });

    describe("#Check level", () => {
        it("Should check the level for a user", () => {
        	// Test
        });
    });
});
